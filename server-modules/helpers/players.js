/**
 * Checks a player's name against `ops.json` and returns whether or not they're an OP.
 * 
 * @param {String} playerName The name to check
 * @return {Boolean}
 */
const isOp = function (playerName) {
    return false;
};

module.exports = { isOp };
